export const MIN_SIZE = 2;
export const DEFAULT_SIZE = 15;
export const SORT_SPEED = 150;

export enum SortStatuses {
    started = 'started',
    notStarted = 'not started',
    paused = 'paused',
    completed = 'completed',
};
